1. pip install streamlit pandas requests
2. Create an virtual 
python -m venv venv
3.Activate 
venv/Scripts\active 
4.Install Dependencies
pip install streamlit pandas requests python-dotenv
5. Verify is installed
streamlit hello